<%= form_with model: [@post.author, @post, @comment], url: user_post_comments_path(@post.author, @post) do |form| %>
  <%= form.label :text %>
  <%= form.text_area :text %>

  <%= form.submit "Add Comment" %>
<% end %>