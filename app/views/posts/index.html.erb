<%= render "shared/user_box", user: @user %>

<% @posts.includes(:comments, :author).each do |post| %>
  <%= render "shared/post_box", post: post %>
  <%= render "shared/comment_box", comments: post.recent_comments.includes(:author) %>
<% end %>

<a class="a-btn" href="#">Pagination</a>
